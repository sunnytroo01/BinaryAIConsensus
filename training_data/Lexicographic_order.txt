Lexicographic order Grokipedia
 

 Fact-checked by Grok 1 month ago 
Lexicographic order
 Ara Eve Leo Sal 
1x
 Lexicographic order, also known as dictionary order or lexical order, is a total order on the Cartesian product of totally ordered sets, where elements (such as tuples or sequences) are compared componentwise from left to right, with the order determined by the first position at which they differ. For two elements (a1,a2,,an)(a_1, a_2, \dots, a_n)(a1,a2,,an) and (b1,b2,,bm)(b_1, b_2, \dots, b_m)(b1,b2,,bm) in the product space, with nmn \leq mnm without loss of generality, the first is less than the second if ai<bia_i < b_iai<bi at the smallest index iii where they differ, or if they agree on the first nnn components and the first sequence is shorter. Equality holds if the sequences are identical. This ordering is reflexive, antisymmetric, transitive, and total, making it a linear extension suitable for sorting and comparison tasks.
The lexicographic order generalizes the familiar alphabetical arrangement of words, where strings are ordered by successive characters, and extends naturally to infinite sequences or ordinals in set theory and topology. In mathematics, it plays a fundamental role in defining monomial orders for polynomial rings, particularly in computational algebra, where the lex order (with variables ordered x1>x2>>xnx_1 > x_2 > \dots > x_nx1>x2>>xn) is used to compute Grbner bases for solving systems of polynomial equations. For instance, in the lex order, monomials are compared by the highest-degree variable first, breaking ties with subsequent variables, which facilitates elimination algorithms akin to Gaussian elimination for multivariables.
Beyond algebra, lexicographic order finds applications in combinatorics for enumerating objects like permutations and trees, where it induces well-ordered structures isomorphic to subsets of reals or ordinals. In optimization and constraint programming, it supports multicriteria decision-making by prioritizing objectives in a hierarchical manner, as seen in lexicographic optimization for inverse problems in radiotherapy planning. Additionally, in topology, the order topology generated by a lexicographic order on the unit square provides a compact Hausdorff but non-metrizable space, while the long lineconstructed as the lexicographic order on [0,1)[0,1)[0, \omega_1) \times [0,1)[0,1)[0,1), where 1\omega_11 is the first uncountable ordinalyields a non-metrizable, non-separable ordered topological space.

Definition and Properties

Formal Definition

The lexicographic order, also known as dictionary order or lex order, is a total order defined on the Cartesian product of totally ordered sets. For two totally ordered sets (X,<)(X, <)(X,<) and (Y,<)(Y, <)(Y,<), the lexicographic order <lex<_{\text{lex}}<lex on XYX \times YXY is defined by (x1,y1)<lex(x2,y2)(x_1, y_1) <_{\text{lex}} (x_2, y_2)(x1,y1)<lex(x2,y2) if either x1<x2x_1 < x_2x1<x2, or x1=x2x_1 = x_2x1=x2 and y1<y2y_1 < y_2y1<y2. This definition extends recursively to the finite Cartesian product X1XnX_1 \times \cdots \times X_nX1Xn of totally ordered sets, where for tuples a=(a1,,an)a = (a_1, \dots, a_n)a=(a1,,an) and b=(b1,,bn)b = (b_1, \dots, b_n)b=(b1,,bn), a<lexba <_{\text{lex}} ba<lexb if there exists a smallest index kkk such that ai=bia_i = b_iai=bi for all i<ki < ki<k and ak<bka_k < b_kak<bk.
For finite sequences of equal length drawn from a totally ordered set AAA, the lexicographic order compares them componentwise from the left until the first differing position, as in the tuple case above. To extend this to sequences of unequal lengths, such as words over an ordered alphabet, one common convention is to compare up to the length of the shorter sequence; if one sequence is a proper prefix of the other, the shorter sequence precedes the longer one. Formally, for sequences X=x1xrX = x_1 \dots x_rX=x1xr and Y=y1ysY = y_1 \dots y_sY=y1ys with rsr \leq srs, X<YX < YX<Y if there exists trt \leq rtr such that xi=yix_i = y_ixi=yi for iti \leq tit, and either t<rt < rt<r and xt+1<yt+1x_{t+1} < y_{t+1}xt+1<yt+1, or t=r<st = r < st=r<s (making XXX a proper prefix of YYY).

Basic Properties

The lexicographic order on the Cartesian product of totally ordered sets inherits the totality of the underlying orders, ensuring that for any two distinct elements aaa and bbb, exactly one of a<ba < ba<b, b<ab < ab<a, or a=ba = ba=b holds. This property arises because the comparison begins with the first differing component, where the underlying total order decides the relation definitively.
The lexicographic order is a total order, satisfying reflexivity (aaa \leq aaa for all aaa) and antisymmetry (if aba \leq bab and bab \leq aba, then a=ba = ba=b). It is also transitive: if a<ba < ba<b and b<cb < cb<c, then a<ca < ca<c, as the comparison rule propagates the inequalities through the components without contradiction.
The lexicographic order is compatible with the orders on each individual component, preserving their relations in the product; specifically, if two elements agree on initial components and differ later, the order respects the component-wise ordering at the point of difference. This compatibility ensures that the lexicographic order extends the original orders naturally.
When each component set is well-ordered, the lexicographic order on their finite Cartesian product is also well-ordered, meaning every nonempty subset has a least element, with the order type being the ordinal product of the component order types.

Applications in Sequences

Ordering of Strings and Words

Lexicographic order, also known as dictionary order, is applied to strings or words formed from an alphabet, which is a finite set of symbols equipped with a total order, such as the standard ordering a<b<<za < b < \dots < za<b<<z on the English alphabet. A word over this alphabet is a finite sequence of symbols from it, and the set of all such finite words forms the free monoid generated by the alphabet under the operation of concatenation.
To compare two finite words uuu and vvv in lexicographic order, one examines them position by position from the left until a difference is found: if the symbols differ at the first such position, the word with the smaller symbol there is considered smaller; if one word is a proper prefix of the other, the shorter word is smaller. For example, the word "apple" precedes "apricot" because the first two letters match ("ap"), but the third letter 'p' in "apple" is less than 'r' in "apricot". This ordering extends the total order on the alphabet to a total order on the free monoid of words, which is compatible with concatenation in the sense that it respects the monoid structure (with further details on the algebraic properties provided in the section on the monoid of words).
The lexicographic order can also be extended to infinite words, which are sequences of arbitrary (possibly infinite) length over the alphabet, including right-infinite strings. For two infinite words, comparison proceeds similarly by scanning from the left until the first differing position, where the word with the smaller symbol at that position is deemed smaller; since both are infinite, there is no prefix issue unless they are identical up to that point. This extension preserves the total order property and is fundamental in the study of infinite sequences in combinatorics on words.

Numeral Systems and Dates

In positional numeral systems, where digits are ordered from 0 to 9, lexicographic order treats numerical representations as strings, leading to counterintuitive results such as "10" preceding "2" because the first character '1' is less than '2', despite 10 being numerically greater than 2. To align lexicographic sorting with numerical order, numbers are often padded with leading zeros to a fixed width, ensuring that shorter strings do not precede longer ones prematurelyfor instance, formatting "2" as "02" and "10" as "10" yields the correct sequence.
A key application appears in date formatting under the ISO 8601 standard, which uses the YYYY-MM-DD structure; here, lexicographic order matches chronological progression because the year (most significant unit) is represented first with four digits, followed by the month and day each padded to two digits, allowing string comparison to reflect temporal sequencefor example, "2025-01-15" precedes "2025-02-01". Similarly, time representations in HH:MM:SS format, with each component padded to two digits (e.g., "09:05:30"), ensure that lexicographic sorting aligns with temporal order, as earlier hours, minutes, or seconds will have smaller leading digits.
However, without such padding, variable-length numerical strings introduce pitfalls in lexicographic sorting; for example, "9" follows "10" because '9' > '1' at the first position, inverting the numerical order and complicating data processing tasks like file naming or database queries.
Historically, lexicographic principles underpinned early computing data processing through punched card systems from the 1890 U.S. Census onward, where mechanical sorters such as the IBM Type 80 arranged cards column by columnequivalent to digit-by-digit lexicographic orderingto sort numerical fields such as employee numbers or dates.

Lexicographic Ordering of Permutations

The lexicographic order is widely used in combinatorics to enumerate all permutations of a finite set of nnn distinct elements, assumed to be ordered as 1<2<<n1 < 2 < \dots < n1<2<<n. The permutations are ordered such that the first permutation is the identity (1,2,,n)(1, 2, \dots, n)(1,2,,n), followed by those that differ in the last position, and so on, progressing through systematic changes from the right until carrying over to earlier positions.
A key algorithmic aspect is the computation of the kkk-th permutation in this order (typically 0-indexed), which can be efficiently determined using the factorial number system, also known as the factoradic representation. In this system, any integer kkk (with 0k<n!0 \leq k < n!0k<n!) is uniquely expressed as k=dn1(n1)!+dn2(n2)!++d11!+d00!k = d_{n-1} (n-1)! + d_{n-2} (n-2)! + \dots + d_1 \cdot 1! + d_0 \cdot 0!k=dn1(n1)!+dn2(n2)!++d11!+d00!, where 0dii0 \leq d_i \leq i0dii. To generate the kkk-th permutation, start with the sorted list of elements. For each position iii from left to right (corresponding to factorials from (n1)!(n-1)!(n1)! down to 1!1!1!), select the dnid_{n-i}dni-th remaining unused element (0-indexed) for that position, and remove it from the list. This process yields the permutation in O(n2)O(n^2)O(n2) time, though optimized implementations achieve O(n)O(n)O(n) with appropriate data structures.
Conversely, the lexicographic rank (or index) of a given permutation \pi is the number of permutations that precede it in the order, computed as i=1nci(ni)!\sum_{i=1}^{n} c_i \cdot (n-i)!i=1nci(ni)!, where cic_ici is the number of unused elements smaller than i\pi_ii at position iii. This ranking can be performed in O(n2)O(n^2)O(n2) time by scanning the remaining elements at each step. These procedures are fundamental in combinatorial generation and are detailed in Knuth's algorithms, such as Algorithm L for generating all permutations in lexicographic order.
This framework extends to injective functions from a set of size nnn to a totally ordered codomain of size mnm \geq nmn, equivalent to choosing and ordering nnn distinct elements from the codomain in lexicographic order. The ranking and unranking then involve multinomial coefficients instead of pure factorials, but the principles remain analogous.

Product and Algebraic Structures

Cartesian Product of Ordered Sets

The lexicographic order extends naturally to the finite Cartesian product of totally ordered sets. Given totally ordered sets X1,,XnX_1, \dots, X_nX1,,Xn with respective orders 1,,n\leq_1, \dots, \leq_n1,,n, the lexicographic order lex\leq_{\mathrm{lex}}lex on X=X1XnX = X_1 \times \cdots \times X_nX=X1Xn compares elements (x1,,xn)(x_1, \dots, x_n)(x1,,xn) and (y1,,yn)(y_1, \dots, y_n)(y1,,yn) by finding the smallest index kkk where xkykx_k \neq y_kxk=yk; then (x1,,xn)lex(y1,,yn)(x_1, \dots, x_n) \leq_{\mathrm{lex}} (y_1, \dots, y_n)(x1,,xn)lex(y1,,yn) if xkkykx_k \leq_k y_kxkkyk, or if the tuples are identical. This construction yields a total order on XXX whenever each XiX_iXi is totally ordered, as the comparison always resolves via the first differing component.
The definition builds iteratively on the two-set case: the order on X1X2X_1 \times X_2X1X2 serves as the foundation, with longer products obtained by treating (X1Xn1)Xn(X_1 \times \cdots \times X_{n-1}) \times X_n(X1Xn1)Xn and applying the pairwise rule, where the prior product acts as the primary coordinate. For illustration, consider the product (/page/R)(/page/Z){0,1}(/page/R) \times (/page/Z) \times \{0,1\}(/page/R)(/page/Z){0,1}, where (/page/R)(/page/R)(/page/R) and (/page/Z)(/page/Z)(/page/Z) carry their standard orders, and {0,1}\{0,1\}{0,1} is ordered as 0<10 < 10<1. Triples (x,y,z)(x,y,z)(x,y,z) and (x,y,z)(x',y',z')(x,y,z) satisfy (x,y,z)<lex(x,y,z)(x,y,z) <_{\mathrm{lex}} (x',y',z')(x,y,z)<lex(x,y,z) if x<xx < x'x<x, or x=xx = x'x=x and y<yy < y'y<y, or x=xx = x'x=x, y=yy = y'y=y, and z<zz < z'z<z. This prioritizes the real component, then the integer, then the boolean, mimicking dictionary ordering across heterogeneous types.
Regarding order types, the lexicographic product combines the types of the components in a non-commutative way, with the initial coordinates dominating. For example, the order type of Z\omega \times \mathbb{Z}Z under lexicographic orderwhere \omega is the type of the natural numbers and Z\mathbb{Z}Z that of the integersis not well-ordered, despite \omega being well-ordered. The sequence (0,0)>lex(0,1)>lex(0,2)>lex(0,0) >_{\mathrm{lex}} (0,-1) >_{\mathrm{lex}} (0,-2) >_{\mathrm{lex}} \cdots(0,0)>lex(0,1)>lex(0,2)>lex forms an infinite descending chain, as the first components match while the second decrease without bound. In general, for finite products, well-ordering holds if and only if every component is well-ordered, but the resulting type depends on the sequence of components.
This construction is restricted to finite nnn, as infinite products under lexicographic order may fail to be total or well-ordered even when components are ordinals, though such cases lie beyond the finite setting.

Monoid of Words

In the context of formal language theory, the free monoid AA^*A generated by a finite ordered alphabet AAA consists of all finite sequences of elements from AAA, known as words, including the empty word \epsilon, under the associative operation of concatenation, which has \epsilon as the identity element. The lexicographic order on AA^*A is defined by comparing words position by position: for distinct words u=u1u2umu = u_1 u_2 \cdots u_mu=u1u2um and v=v1v2vnv = v_1 v_2 \cdots v_nv=v1v2vn, the first position iii where uiviu_i \neq v_iui=vi determines the order, with u<vu < vu<v if ui<viu_i < v_iui<vi in the order on AAA; if one word is a proper prefix of the other (say m<nm < nm<n and ui=viu_i = v_iui=vi for i=1,,mi = 1, \dots, mi=1,,m), then the shorter word precedes the longer one.
This order is a total order on AA^*A and is compatible with the monoid operation of concatenation in the following sense: if u<vu < vu<v, then for any word wAw \in A^*wA, both uw<vwuw < vwuw<vw and wu<wvwu < wvwu<wv. This compatibility arises because the initial differing symbols in uuu and vvv remain the first point of difference after appending or prepending www, preserving the relative order. The prefix property ensures that shorter words precede their proper extensions, as the shorter word can be viewed as padded with an implicit element smaller than all in AAA beyond its length.
For example, consider the binary alphabet A={0<1}A = \{0 < 1\}A={0<1}. The words 001001001 and 010101 are compared by aligning prefixes: the first two symbols are 000000 versus 010101, and since the second symbols satisfy 0<10 < 10<1, it follows that 001<01001 < 01001<01.
The lexicographic order on AA^*A plays a key role in formal language theory for systematically generating and enumerating words, such as listing all words of a regular language in order without duplicates or omissions, which facilitates algorithms for language recognition and decision problems.

Advanced Mathematical Contexts

Lexicographic Order on Functions

The lexicographic order on the set of functions from a well-ordered set DDD to an ordered set RRR is defined by declaring two functions f,g:DRf, g: D \to Rf,g:DR to satisfy f<lexgf <_{\mathrm{lex}} gf<lexg if there exists a least element dDd \in DdD such that f(d)g(d)f(d) \neq g(d)f(d)=g(d) and f(d)<g(d)f(d) < g(d)f(d)<g(d) in the order on RRR. This extends the familiar dictionary order to arbitrary well-ordered domains, ensuring the comparison begins at the minimal point of disagreement.
When RRR itself is well-ordered, the lexicographic order equips the full function space RDR^DRD with a well-ordering, as every nonempty subset has a least element by transfinite induction along DDD: subsets with agreement up to any initial segment reduce to the order on RRR at the next point. The order type of this well-ordering is the ordinal power Rot(D)|R|^{\mathrm{ot}(D)}Rot(D) in the sense of ordinal exponentiation.
A key special case arises with functions of finite support, meaning f(d)=0Rf(d) = 0_Rf(d)=0R (the minimal element of RRR) for all but finitely many dDd \in DdD. The lexicographic order restricts naturally to this subset, preserving well-orderedness when RRR is well-ordered, and it models structures like formal power series or multiindices in algebra. For instance, univariate polynomials with coefficients in a well-ordered ring can be identified with finite-support functions NR\mathbb{N} \to RNR via their coefficient sequences, ordered lexicographically by comparing coefficients starting from the constant term; this induces a total order on the polynomials reflecting their functional behavior on N\mathbb{N}N.
This construction ties directly to ordinal arithmetic: for ordinals =R\alpha = |R|=R and =ot(D)\beta = \mathrm{ot}(D)=ot(D), the lexicographic order on RDR^DRD realizes the exponentiation \alpha^\beta, where the full space captures the complete hierarchical structure beyond finite-support approximations.
However, if RRR is not well-ordered, the lexicographic order on RDR^DRD need not be a well-ordering. For example, on Z\mathbb{Z}^\omegaZ (integer sequences indexed by naturals, with the standard order on Z\mathbb{Z}Z), consider the descending chain vnv_nvn where vn(k)=1v_n(k) = 1vn(k)=1 if k=nk = nk=n and 000 otherwise for n<n < \omegan<; then vn>lexvn+1v_n >_{\mathrm{lex}} v_{n+1}vn>lexvn+1 since they agree up to index n1n-1n1 and vn(n)=1>0=vn+1(n)v_n(n) = 1 > 0 = v_{n+1}(n)vn(n)=1>0=vn+1(n), yielding an infinite descending chain with no minimal element.

Ordering of Finite Subsets

Given a totally ordered set XXX, the lexicographic order on the collection of all finite subsets of XXX is induced by representing each finite subset SXS \subseteq XSX as the strictly increasing tuple of its elements (sorted according to the order on XXX), and then applying the lexicographic order to these tuples. This representation ensures a one-to-one correspondence between finite subsets and finite increasing sequences from XXX, allowing the lexicographic order on tuplesdefined earlier in the context of Cartesian products of ordered setsto extend naturally to subsets.
For example, consider X={1,2,3}X = \{1, 2, 3\}X={1,2,3} with the standard ordering. The finite subsets in lexicographic order are <{1}<{1,2}<{1,3}<{2}<{2,3}<{3}\emptyset < \{1\} < \{1,2\} < \{1,3\} < \{2\} < \{2,3\} < \{3\}<{1}<{1,2}<{1,3}<{2}<{2,3}<{3}, where each subset is viewed as its sorted tuple: ()(\,)() for \emptyset, (1)(1)(1) for {1}\{1\}{1}, (1,2)(1,2)(1,2) for {1,2}\{1,2\}{1,2}, and so on. This ordering prioritizes subsets by their smallest differing element, mimicking dictionary order on the sorted representations.
When restricted to subsets of fixed cardinality kkk, this yields the standard lexicographic order on kkk-combinations of XXX, establishing a bijection between such subsets and increasing kkk-tuples ordered lexicographically. In combinatorics, this ordering facilitates systematic enumeration of subsets; algorithms for generating all finite subsets or kkk-subsets in lexicographic order enable efficient traversal for problems like combinatorial search and ranking/unranking. For instance, Knuth's Algorithm L produces combinations in this order, supporting applications in exhaustive generation with minimal changes between successive outputs.
Unlike the subset inclusion order, which is merely a partial order (as incomparable subsets like {1,3}\{1,3\}{1,3} and {2}\{2\}{2} exist), the lexicographic order provides a total order on all finite subsets, ensuring every pair is comparable.

Orders on Cyclic Groups

The cyclic group Z/nZ\mathbb{Z}/n\mathbb{Z}Z/nZ is typically equipped with the standard total order 0<1<<n10 < 1 < \dots < n-10<1<<n1, which arises from its natural embedding as a quotient of the ordered additive group Z\mathbb{Z}Z. This order allows the group elements to be treated as symbols in an ordered alphabet for lexicographic purposes. However, this linear order is not invariant under the group addition (translation by a fixed element), as, for example, adding 1 modulo nnn maps the minimal element 0 to 1 while sending n1n-1n1 to 0, disrupting the order structure.
For products Znk\mathbb{Z}_n^kZnk, the standard lexicographic order is defined componentwise: two tuples (a1,,ak)(a_1, \dots, a_k)(a1,,ak) and (b1,,bk)(b_1, \dots, b_k)(b1,,bk) satisfy (a1,,ak)<(b1,,bk)(a_1, \dots, a_k) < (b_1, \dots, b_k)(a1,,ak)<(b1,,bk) if at the first position iii where aibia_i \neq b_iai=bi, we have ai<bia_i < b_iai<bi in the standard order on Zn\mathbb{Z}_nZn. To incorporate the cyclic group structure and achieve rotational invarianceessential for applications respecting cyclic shiftsa modified version known as the cyclic lexicographic order is used. In this order, each tuple is represented by its cyclic shift that is minimal under the standard lex order, and tuples are then compared based on these minimal representatives. This ensures the ordering is invariant under the action of the cyclic shift operator, preserving group symmetries while maintaining a total order.
Lex-compatible group orders on Zn\mathbb{Z}_nZn or its powers extend this framework by requiring that the order interacts compatibly with the group operation, such as preserving inequalities under addition in a restricted sense (e.g., for elements in a positive cone where a<ba < ba<b implies a+c<b+ca + c < b + ca+c<b+c for certain ccc). Such orders are constructed via lexicographic products or liftings to ordered covers like Z\mathbb{Z}Z, but finite cyclicity limits full bi-invariance, often resulting in partial compatibility rather than strict preservation. In coding theory, this adapted lex order facilitates the enumeration and decoding of cyclic codes, where codewordsviewed as tuples in Zqn\mathbb{Z}_q^nZqn for some qqqare normalized to their lex-minimal cyclic shift to eliminate redundancies under rotation, enabling efficient Grbner basis computations for error correction up to the code's designed distance. For instance, in the syndrome decoding of BCH cyclic codes, the lex order on syndrome polynomials helps solve key equations via reduced Grbner bases.
A key limitation of the pure standard lex order on Znk\mathbb{Z}_n^kZnk is its failure to respect cyclic shifts inherent to the group structure: a tuple and its rotation may occupy unrelated positions in the ordering, complicating symmetry-aware applications like codeword identification. Rotational adaptations mitigate this but introduce computational overhead for finding minimal shifts, particularly for large nnn or kkk. These orders thus balance conceptual simplicity with practical invariance needs in algebraic contexts.

Colexicographic Order

The colexicographic order, commonly abbreviated as colex order, is a total order on finite sequences (or tuples) from a totally ordered set, obtained by comparing elements starting from the rightmost position. For two tuples a=(a1,a2,,an)a = (a_1, a_2, \dots, a_n)a=(a1,a2,,an) and b=(b1,b2,,bn)b = (b_1, b_2, \dots, b_n)b=(b1,b2,,bn) of equal length, a<ba < ba<b if the largest index kkk where akbka_k \neq b_kak=bk satisfies ak<bka_k < b_kak<bk; shorter sequences are typically padded with minimal elements (such as zeros) on the left to equalize lengths. This contrasts with the standard lexicographic order by reversing the direction of comparison, effectively prioritizing the least significant components first.
Equivalently, the colex order on sequences is the lexicographic order applied to their reverses, transforming it into a "co-" (or reverse) variant suitable for right-to-left scanning. On words over an alphabet with a total order, colex induces a reverse dictionary ordering, where sequences are compared as if read backward, making it ideal for applications requiring enumeration in decreasing order of significance from the end.
Like lexicographic order, colex is a total orderreflexive, antisymmetric, transitive, and linearensuring every pair of distinct sequences is comparable, but it inverts positional priorities such that changes in trailing elements take precedence over leading ones. This property facilitates efficient adjacent transpositions in generation algorithms, as successive elements in colex often differ only in the rightmost positions.
For example, consider the tuples (1,3,2)(1,3,2)(1,3,2) and (1,2,3)(1,2,3)(1,2,3) over the natural numbers. They agree at index 1 but differ at indices 2 (3>23 > 23>2) and 3 (2<32 < 32<3); the largest differing index is 3, where 2<32 < 32<3, so (1,3,2)<(1,2,3)(1,3,2) < (1,2,3)(1,3,2)<(1,2,3) in colex order.
In combinatorial generation, colex order excels for producing combinations and subsets, enabling compact ranking (mapping a combination to its position) and unranking (the reverse) via simple arithmetic on binomial coefficients, often outperforming lex in speed and code simplicity for large sets. It is particularly effective for algorithms that build structures recursively from the largest elements, such as generating kkk-subsets of {1,2,,n}\{1, 2, \dots, n\}{1,2,,n} where adjacent subsets in colex differ by replacing one element, minimizing computational overhead.

Monomial Orders

In the polynomial ring kkk over a field kkk, monomials are of the form x=x11xnnx^\alpha = x_1^{\alpha_1} \cdots x_n^{\alpha_n}x=x11xnn where =(1,,n)Nn\alpha = (\alpha_1, \dots, \alpha_n) \in \mathbb{N}^n=(1,,n)Nn is the exponent vector. A monomial order is a total order on these monomials that is compatible with multiplication (if x>xx^\alpha > x^\betax>x, then xx>xxx^\alpha x^\gamma > x^\beta x^\gammaxx>xx for all \gamma) and forms a well-order, ensuring no infinite descending chains (Artinian property). This well-ordering is crucial for the termination of algorithms in computational algebra, such as polynomial division and the computation of Grbner bases for ideals.
The pure lexicographic order (often denoted lex), assuming a variable ordering x1>x2>>xnx_1 > x_2 > \dots > x_nx1>x2>>xn, compares exponent vectors from left to right: x>lexxx^\alpha >_\text{lex} x^\betax>lexx if, in the leftmost component where ii\alpha_i \neq \beta_ii=i, we have i>i\alpha_i > \beta_ii>i. This order respects the variable hierarchy, prioritizing differences in the highest-indexed variable first.
Common variants adapt the lexicographic comparison for efficiency in Grbner basis computations. The reverse lexicographic order (revlex) compares vectors from right to left, starting with the lowest-indexed variable: x>revlexxx^\alpha >_\text{revlex} x^\betax>revlexx if the rightmost differing component has i>i\alpha_i > \beta_ii>i. The graded reverse lexicographic order (grevlex) first compares total degrees i>i\sum \alpha_i > \sum \beta_ii>i, and for ties, applies revlex to break them. These variants maintain the Artinian property while often yielding sparser leading terms in Grbner bases compared to pure lex.
For example, in Q\mathbb{Q}Q with x>yx > yx>y, consider x2yx^2 yx2y with exponent (2,1)(2, 1)(2,1) and xy2x y^2xy2 with (1,2)(1, 2)(1,2). Under pure lex, the first components differ with 2>12 > 12>1, so x2y>lexxy2x^2 y >_\text{lex} x y^2x2y>lexxy2. The colexicographic order, a related variant, reverses the comparison direction entirely by starting from the last variable.

References

5.4 New Orders from Old

 Order Relations

 Lexicographic order These notes are about an example that isn't ...

 1 Introduction

 Groebner Bases and Applications

 Lesson 7 Monomial Orderings and the Division Algorithm

 Lexicographic ordering: intuitive multicriteria optimization for IMRT

 Section 14. The Order Topology

 10. Orders and 1

 4. Partial Orderings

 RELATIONS - DePaul University

 Chapter 5 Partial Orders, Lattices, Well Founded Orderings ...

245B, Notes 7: Well-ordered sets, ordinals, and Zorn's lemma ...

Combinatorics on Words - M. Lothaire - Google Books

 Combinatorics on Words A Tutorial *

Lexicographic Ordering | Baeldung on Computer Science

Why do some sorting methods sort by 1, 10, 2, 3...?

How to Sort Number Strings Numerically - DZone

Saving Numbers as VARCHAR: A Hidden Problem with Sorting and ...

ISO 8601: The global standard for date and time formats - IONOS

Date Formats: ISO 8601 - SRCco.de

Sort Date Strings in Java | Baeldung

Inside card sorters: 1920s data processing with punched cards and ...

Punched Card Data Processing - School of Computer Science

lexicographic order in nLab

Lexicographic Order - an overview | ScienceDirect Topics

product of posets - PlanetMath.org

Lexicographic Order of Family of Well-Ordered Sets is not ...

Order type - Encyclopedia of Mathematics

lexicographic order in nLab

 THE GROUPS OF RICHARD THOMPSON AND COMPLEXITY

 Generating Subsets

 Chapter 8 Ordered Sets

Lexicographic Order -- from Wolfram MathWorld

 Lecture 3: Combinatorial Generation Steven Skiena Department of ...

Generate combinations - The Combinatorial Object Server

 Logic and Set Theory Examples 1

 The sandpile group of a tree - Cornell Mathematics

 cyclically ordered groups, first order theory, orderable, universal - HAL

 arXiv:1311.0499v1 3 Nov 2013

On the Decoding of Cyclic Codes Using Grbner Bases

Monomial Orders - Maple Help - Maplesoft

 1 Lecture 14 Monomial orderings

Term orders - Polynomials

What does reverse lexicographic order mean? - Math Stack Exchange

The Art of Computer Programming

The Number System of the Permutations Generated by Cyclic Shift

Efficient Algorithms to Rank and Unrank Permutations in Lexicographic Order
 
Table of Contents
 

 
Sign in to contribute
 
Create an account or sign in to suggest articles and edits to Grokipedia.
 
Sign in
 
Suggest an article
 
Know something the world should know? Tell us what to write about.
 New Article Suggest Edit Topic (optional if you add details) Details (optional if you add a topic) 
What makes a great suggestion?
 
Specific beats broad "CRISPR" over "Biology"
 
People, events, and breakthroughs are ideal
 
Search first to check if it already exists
 Cancel Submit Summary Edit content (optional) Supporting sources (optional) 
Add another source
 
What makes a great edit?
 
Select the wrong text in the article first
 
Add a source link so we can verify
 
One fix per submission is easiest to review
 Cancel Submit Edit 

 
Something went wrong
 
We couldn't submit your suggestion. Please try again.
 
Try again
 
Thank you!
 
Grok will review your suggestion and add the article if it sees fit.
 
View my suggestions
 
Submit another suggestion